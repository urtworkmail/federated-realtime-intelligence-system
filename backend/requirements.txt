fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Database drivers
asyncpg==0.30.0
aiomysql==0.2.0
neo4j==5.26.0
motor==3.6.0
oracledb==2.5.0

# Cloud data warehouse drivers (lazily imported by connectors/warehouses.py —
# the app boots without them; a connector using one raises a clear install hint).
# The Salesforce/HubSpot CRM connectors are pure-httpx and need no extra driver.
snowflake-connector-python==3.12.3
google-cloud-bigquery==3.27.0

# MCP (Model Context Protocol) server SDK — exposes the tenant-scoped read
# surface as MCP tools. Lazily imported by api/mcp_server.py.
mcp>=1.2.0,<2.0.0

# Embeddings — offline-deployable, CPU-friendly model for entity semantic search
sentence-transformers==3.3.1

# Streaming
aiokafka==0.11.0
websockets==13.1

# HTTP and scraping
httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0

# File parsing
openpyxl==3.1.5
chardet==5.2.0
pypdf==5.1.0

# GraphQL — version range (not pinned exact) since release cadence is fast;
# any 0.230+ series supports the FastAPI integration and JSON scalar used here.
strawberry-graphql[fastapi]>=0.230.0,<1.0.0

# RSS
feedparser==6.0.11

# Utilities
python-multipart==0.0.12
python-dotenv==1.0.1

# Auth / secrets / billing
pydantic-settings==2.6.1
email-validator==2.2.0
cryptography==43.0.3
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

